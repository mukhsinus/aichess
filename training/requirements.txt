# Training pipeline dependencies (install on top of the main project's requirements)
ultralytics>=8.4.0,<9.0
opencv-python>=4.13.0,<5.0
numpy>=2.4.0,<3.0
albumentations>=2.0.0
requests>=2.31.0
PyYAML>=6.0
certifi>=2024.0.0

# Optional: install for broader dataset source support
# huggingface_hub>=0.20.0   # enables HuggingFace snapshot downloads
# datasets>=3.0.0           # enables HuggingFace datasets library
# kaggle>=1.6.0             # enables Kaggle downloads
